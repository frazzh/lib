React Bootstrap modal component----------------------------
-used to display some dialogs be it notifiation ,information etc

-"Intl.DateTimeFormat"-Intl.DateTimeFormat is used to format date strings in JavaScript.Here we have used custom date formatting where we have decided the format of date by ourselves

-window.scrollTo(xcoordinate,y-coordinate) is used to slide  horixontally or vert

-So basically routing is a mapping between an URL and the functionality of a web server